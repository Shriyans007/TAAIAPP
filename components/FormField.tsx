import { Text, TextInput, TextInputProps, View } from 'react-native'; import { colors, radius, spacing } from '@/theme';
export function FormField({label,error,...props}:TextInputProps&{label:string;error?:string}){return <View style={{gap:spacing.xs}}><Text style={{fontWeight:'600',color:colors.primaryDark}}>{label}</Text><TextInput accessibilityLabel={label} {...props} style={[{minHeight:48,borderWidth:1,borderColor:error?colors.error:colors.border,borderRadius:radius.lg,paddingHorizontal:spacing.md,backgroundColor:colors.surface,color:colors.textPrimary},props.style]}/>{error&&<Text accessibilityRole="alert" style={{color:colors.error,fontSize:12}}>{error}</Text>}</View>}
