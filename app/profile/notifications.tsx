import { useState } from 'react';import { Switch,Text,View } from 'react-native';import { Button,Screen } from '@/components';import { useAuth } from '@/services/auth/AuthProvider';import { NotificationPreferences,registerNotifications } from '@/services/notifications/register';import { colors,spacing } from '@/theme';
export default function NotificationSettings(){const {token}=useAuth();const [prefs,setPrefs]=useState<NotificationPreferences>({events:true,community:true,membership:true});const [message,setMessage]=useState('');return <Screen title="Notifications"><Text>Choose which useful TAAI updates you want to receive.</Text>{Object.entries(prefs).map(([key,value])=><View key={key} style={{flexDirection:'row',justifyContent:'space-between',alignItems:'center',paddingVertical:spacing.md}}><Text style={{textTransform:'capitalize'}}>{key==='membership'?'Membership reminders':`${key} updates`}</Text><Switch accessibilityLabel={`${key} notifications`} value={value} trackColor={{true:colors.primary}} onValueChange={x=>setPrefs({...prefs,[key]:x})}/></View>)}<Button label="Save Notification Preferences" disabled={!token} onPress={async()=>{if(token){const r=await registerNotifications(token,prefs);setMessage(r.granted?'Preferences saved.':'Notifications remain off. You can enable them in device settings.')}}}/>{!!message&&<Text>{message}</Text>}</Screen>}

