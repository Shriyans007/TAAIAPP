# Account deletion

The user enters their current password and accepts an explicit destructive confirmation. The server rejects administrator deletion, revokes sessions, deletes push tokens, erases billing/shipping profile metadata, anonymises identity fields and disables future mobile login. WooCommerce orders and transaction records are retained where legal/accounting obligations apply. TAAI must review this retention behaviour with its privacy/legal adviser before release.
