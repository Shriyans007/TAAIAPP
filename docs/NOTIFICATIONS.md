# Notifications

Set a real EAS project ID, build on a physical device, sign in, and save notification preferences. The Expo push token is stored against the WordPress user. Administrators send from TAAI Mobile in wp-admin to everyone or active members. Production hardening should process Expo receipts asynchronously and delete `DeviceNotRegistered` tokens.

